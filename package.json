{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "raytask",
  "title": "RayTask",
  "description": "A Google Tasks integration for Raycast",
  "icon": "icon.png",
  "author": "balazstasi",
  "version": "1.0.0",
  "license": "MIT",
  "categories": [
    "Productivity"
  ],
  "platforms": [
    "macOS"
  ],
  "preferences": [
    {
      "name": "googleClientId",
      "type": "password",
      "required": false,
      "title": "Google OAuth Client ID",
      "description": "From Google Cloud Console: Credentials → OAuth client (iOS, Bundle ID com.raycast). See Raycast guide: developers.raycast.com/utilities/oauth/getting-google-client-id",
      "placeholder": "xxxx.apps.googleusercontent.com"
    }
  ],
  "commands": [
    {
      "name": "index",
      "title": "Google Tasks",
      "description": "Manage your Google Tasks",
      "mode": "view"
    },
    {
      "name": "create-task",
      "title": "Create Task",
      "description": "Create a task with title, notes, due date, and optional parent",
      "mode": "view"
    },
    {
      "name": "quick-add",
      "title": "Quick Add Task",
      "description": "Add a task using arguments (optional ISO due date)",
      "mode": "view",
      "arguments": [
        {
          "name": "title",
          "type": "text",
          "placeholder": "Task title",
          "required": true
        },
        {
          "name": "due",
          "type": "text",
          "placeholder": "Optional due (e.g. 2026-05-15)",
          "required": false
        }
      ]
    },
    {
      "name": "edit-task",
      "title": "Edit Task",
      "description": "Pick a task from a list and edit it",
      "mode": "view"
    },
    {
      "name": "menu-bar",
      "title": "Today's Tasks",
      "description": "Today’s tasks in the menu bar",
      "mode": "menu-bar",
      "interval": "10m"
    }
  ],
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix": "ray lint --fix"
  },
  "dependencies": {
    "@raycast/api": "^1.104.15",
    "@raycast/utils": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "typescript": "^5.8.2"
  }
}
